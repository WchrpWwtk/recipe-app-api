version: "3.9"

services:
  web:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8003:8000"
    volumes:
      - ./recipes:/recipes
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"